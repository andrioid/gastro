#!/usr/bin/env bash
#MISE description="Install gastro to GOBIN via go install"

set -euo pipefail

go install ./cmd/gastro/

GOBIN="${GOBIN:-$(go env GOPATH)/bin}"
echo "Installed gastro to ${GOBIN}"
